# Production Environment Variables
# These should be set in Vercel Dashboard

# NextAuth Configuration - CRITICAL for session management
# NEXTAUTH_URL must match your deployment URL exactly
NEXTAUTH_URL=https://klassenbuch-app-3xol.vercel.app
# NEXTAUTH_SECRET should be a secure random string (use: openssl rand -base64 32)
# This MUST be the same in Vercel environment variables

# API Configuration
NEXT_PUBLIC_API_URL=https://klassenbuch-app-3xol.vercel.app

# Environment
NODE_ENV=production